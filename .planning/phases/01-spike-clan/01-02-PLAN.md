---
phase: 01-spike-clan
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - .planning/phases/01-spike-clan/artifacts/MOR-CLI-TRIAL.md
  - .planning/phases/01-spike-clan/artifacts/sample_fra.mor.cha
autonomous: false
requirements: [SPIKE-02]

must_haves:
  truths:
    - "La commande mor tourne en headless (sans GUI) sur Windows sur un .cha de test"
    - "La sortie contient un tier %mor lisible (tokens POS|lemme, accents français corrects)"
    - "La syntaxe CLI exacte qui marche est documentée pour réutilisation en Plan 03/Phase 2"
  artifacts:
    - path: ".planning/phases/01-spike-clan/artifacts/MOR-CLI-TRIAL.md"
      provides: "Compte-rendu de l'essai CLI mor headless (SPIKE-02)"
      contains: "## Commande retenue"
    - path: ".planning/phases/01-spike-clan/artifacts/sample_fra.mor.cha"
      provides: "Sortie .cha annotée par mor (preuve du tier %mor)"
      contains: "%mor:"
  key_links:
    - from: "commande mor headless (MORLIB / +l grammar_dir)"
      to: "tier %mor dans le fichier de sortie"
      via: "subprocess PowerShell"
      pattern: "%mor:"
---

<objective>
Wave 1 du spike : valider que la commande `mor` de CLAN s'exécute en headless (sans GUI) sur Windows, sur un `.cha` français, et produit un tier `%mor` lisible. Trouver et documenter la syntaxe CLI EXACTE (variable d'environnement `MORLIB` vs flag `+l <grammar_dir>` vs cwd grammaire) — c'est l'incertitude technique principale (Open Question 2 / Assumption A2).

Purpose: SPIKE-02 — Success Criterion 2 du phase goal. La syntaxe qui marche sera réutilisée telle quelle par le wrapper subprocess propre du Plan 03 (D-05).
Output: Compte-rendu CLI + fichier .cha annoté (preuve du %mor).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-spike-clan/01-RESEARCH.md
@.planning/phases/01-spike-clan/01-CONTEXT.md
@.planning/phases/01-spike-clan/01-01-SUMMARY.md

# Plan 01 a fourni : chemin de mor.exe, chemin grammaire fra/, fixtures.
# Recherche : §Invocation `mor` sur Windows (3 approches CLI), §Pitfall 1 (MORLIB introuvable), §Pitfall 2 (encodage), §Pitfall 3 (overwrite +1).
# D-06 : cet essai CLI peut rester exploratoire/jetable ; seul le wrapper du Plan 03 doit être propre.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Essai exploratoire des 3 syntaxes CLI mor et capture du %mor</name>
  <files>.planning/phases/01-spike-clan/artifacts/sample_fra.mor.cha (sortie annotée produite par l'essai)</files>
  <read_first>
    - .planning/phases/01-spike-clan/01-01-SUMMARY.md (chemin réel de mor.exe et du dossier grammaire fra/)
    - .planning/phases/01-spike-clan/01-RESEARCH.md (§Invocation `mor` sur Windows — Approches 1/2/3 ; §Common Pitfalls 1, 2, 3)
  </read_first>
  <action>
    CHECKPOINT exploratoire (D-06, jetable). Claude tente d'abord l'essai via l'outil Bash/PowerShell si mor.exe est accessible ; sinon l'utilisateur exécute les commandes ci-dessous (le binaire Windows peut être hors sandbox). Tester les 3 approches de <how-to-verify> jusqu'à obtenir un tier %mor valide, puis valider via <resume-signal>.
  </action>
  <what-built>
    Essai CLI exploratoire de `mor` headless. Requiert le binaire CLAN installé localement (Plan 01) ; l'environnement sandbox peut ne pas avoir accès au binaire Windows installé hors sandbox — d'où le checkpoint pour exécution/validation par l'utilisateur si nécessaire.
  </what-built>
  <how-to-verify>
    Travailler sur une COPIE de la fixture (Pitfall 3 : `mor +1` écrase l'entrée). Depuis PowerShell, tester dans l'ordre jusqu'à obtenir un tier %mor valide :

    ```powershell
    $mor     = "<chemin mor.exe du Plan 01>"
    $grammar = "<chemin dossier fra du Plan 01>"
    Copy-Item tests/fixtures/sample_fra.cha .planning/phases/01-spike-clan/artifacts/sample_fra.mor.cha -Force

    # Approche A : flag +l grammar_dir
    & $mor +l $grammar +1 .planning/phases/01-spike-clan/artifacts/sample_fra.mor.cha
    # Approche B : variable d'environnement MORLIB
    $env:MORLIB = $grammar
    & $mor +1 .planning/phases/01-spike-clan/artifacts/sample_fra.mor.cha
    # Approche C : cwd = dossier CLAN
    Push-Location (Split-Path $mor); & $mor +1 "<abs path fixture>"; Pop-Location

    Select-String "%mor" .planning/phases/01-spike-clan/artifacts/sample_fra.mor.cha
    ```

    Critère de succès : le fichier de sortie contient des lignes `%mor:` avec des tokens `POS|lemme` français (PAS `%mor: xxx` partout — ce serait le Pitfall 1, MORLIB introuvable). Vérifier que les accents (é, à) ne sont pas corrompus (Pitfall 2 : encodage). Noter QUELLE approche a fonctionné et la commande exacte.
  </how-to-verify>
  <verify>Vérification via <how-to-verify> : `Select-String "%mor:" sample_fra.mor.cha` retourne des tokens `POS|lemme` (pas `xxx`). Validation humaine du résultat (binaire potentiellement hors sandbox).</verify>
  <acceptance_criteria>
    - `.planning/phases/01-spike-clan/artifacts/sample_fra.mor.cha` contient au moins une ligne `%mor:` avec un token de forme `POS|lemme` (ex : `n|chat`)
    - La sortie %mor n'est PAS majoritairement `xxx`/`0` (sinon MORLIB introuvable -> noter et tenter l'approche suivante)
    - Les caractères accentués sont préservés (pas de `Ã©` / `?`)
    - L'approche CLI qui fonctionne est identifiée (A, B ou C) avec la commande exacte
  </acceptance_criteria>
  <done>Une approche CLI headless produit un tier %mor lisible (ou échec documenté) ; commande exacte et extrait %mor capturés.</done>
  <resume-signal>Coller la commande exacte qui a produit le tier %mor (et l'extrait %mor obtenu), puis taper "approved". Si aucune approche ne marche, décrire l'erreur — cela alimentera le verdict No-Go technique.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Documenter le compte-rendu de l'essai CLI mor (MOR-CLI-TRIAL.md)</name>
  <files>.planning/phases/01-spike-clan/artifacts/MOR-CLI-TRIAL.md</files>
  <read_first>
    - .planning/phases/01-spike-clan/01-RESEARCH.md (§Invocation `mor`, §Pitfalls 1-3)
    - .planning/phases/01-spike-clan/artifacts/sample_fra.mor.cha (sortie produite en Task 1)
  </read_first>
  <action>
    Créer `.planning/phases/01-spike-clan/artifacts/MOR-CLI-TRIAL.md` (en français) avec EXACTEMENT ces sections :
    - `# Essai CLI mor headless — SPIKE-02`
    - `## Approches testées` : tableau des 3 approches (A `+l`, B `MORLIB`, C cwd) avec résultat OK/KO de chacune et message d'erreur le cas échéant.
    - `## Commande retenue` : la commande PowerShell EXACTE qui produit le tier %mor (chemins paramétrés). Inclure encodage observé (UTF-8 / CP1252) et si `+1` écrase l'entrée.
    - `## Extrait %mor obtenu` : coller 2-3 lignes `%mor:` réelles issues de sample_fra.mor.cha.
    - `## Implications pour le wrapper (Plan 03)` : traduire la commande retenue en arguments subprocess Python (liste d'args + env var MORLIB si nécessaire), pour réutilisation directe par `run_mor()`.

    Si Task 1 a échoué (aucune approche headless ne marche) : documenter clairement l'échec et marquer SPIKE-02 comme bloquant pour le verdict du Plan 03 (entrée No-Go technique).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if ((Select-String -Path '.planning/phases/01-spike-clan/artifacts/MOR-CLI-TRIAL.md' -Pattern '## Commande retenue' -Quiet) -and (Select-String -Path '.planning/phases/01-spike-clan/artifacts/sample_fra.mor.cha' -Pattern '%mor:' -Quiet)) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - `MOR-CLI-TRIAL.md` contient `## Commande retenue` et une section `## Implications pour le wrapper (Plan 03)` avec les arguments subprocess Python
    - `sample_fra.mor.cha` contient `%mor:`
    - L'encodage de sortie (UTF-8/CP1252) est documenté (alimente le paramètre `encoding=` du wrapper)
  </acceptance_criteria>
  <done>Syntaxe CLI mor headless documentée et traduite en spécification subprocess pour le Plan 03 ; preuve %mor committée.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python/PowerShell -> binaire mor.exe tiers | invocation locale d'un exécutable tiers (CLAN) |
| chemin de grammaire / chemin de fichier .cha -> ligne de commande | chemins passés à mor en arguments |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Elevation of privilege | invocation mor.exe | accept | Binaire CLAN de confiance (BSD-3 TalkBank), exécution locale spike, pas d'entrée distante ni donnée patient. |
| T-01-05 | Tampering | injection d'argument via chemin de fichier | mitigate | Documenter l'usage d'une LISTE d'arguments subprocess (jamais `shell=True`/concat de chaîne) dans la spec wrapper du Plan 03 ; chemins absolus contrôlés, pas d'entrée utilisateur dans ce spike. |
| T-01-06 | Information disclosure | encodage sortie mor | accept | Échantillon public (pas de patient) ; encodage forcé UTF-8 vérifié. |
</threat_model>

<verification>
- `mor` produit un tier %mor lisible sur Windows en headless (ou échec documenté).
- La commande exacte et son équivalent subprocess Python sont écrits pour le Plan 03.
</verification>

<success_criteria>
- SPIKE-02 tranché : la commande mor FR tourne et produit %mor (Success Criterion 2), OU échec documenté alimentant un No-Go technique.
</success_criteria>

<output>
After completion, create `.planning/phases/01-spike-clan/01-02-SUMMARY.md` incluant : approche CLI retenue, commande exacte, encodage, arguments subprocess Python pour le wrapper, et tout blocage rencontré.
</output>
