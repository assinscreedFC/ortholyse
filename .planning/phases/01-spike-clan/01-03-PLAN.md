---
phase: 01-spike-clan
plan: 03
type: tdd
wave: 2
depends_on: [01, 02]
files_modified:
  - app/models/clan_wrapper.py
  - tests/test_clan_spike.py
  - .planning/phases/01-spike-clan/SPIKE-REPORT.md
autonomous: true
requirements: [SPIKE-03]

must_haves:
  truths:
    - "Un parseur Python extrait les tokens %mor d'un fichier .cha annoté"
    - "La MLU en morphèmes est calculée et plausible vs l'oracle MTLN (plage normative d'âge)"
    - "Le test parsing tourne en CI SANS CLAN installé (sur la fixture déterministe)"
    - "Un rapport de spike conclut Go ou No-Go avec stratégie de repli si No-Go"
    - "Le binaire mor est résolu via le pattern find_ffmpeg (frozen -> vendored -> PATH -> littéral)"
  artifacts:
    - path: "app/models/clan_wrapper.py"
      provides: "Amorce réutilisable Phase 2 : find_clan_mor, run_mor, parse_mor_tier, compute_mlu_morphemes"
      contains: "def parse_mor_tier("
    - path: "tests/test_clan_spike.py"
      provides: "Tests unitaires parsing + MLU (CI sans CLAN) + intégration gated mor"
      contains: "def test_mlu_morphemes_vs_oracle"
    - path: ".planning/phases/01-spike-clan/SPIKE-REPORT.md"
      provides: "Rapport Go/No-Go final (Success Criterion 4)"
      contains: "## Verdict"
  key_links:
    - from: "app/models/clan_wrapper.py::find_clan_mor"
      to: "operation_fichier.find_ffmpeg (pattern de résolution binaire)"
      via: "frozen _MEIPASS -> vendored bin/ -> shutil.which -> littéral"
      pattern: "sys, 'frozen'"
    - from: "compute_mlu_morphemes(cha_path)"
      to: "tier %mor parsé"
      via: "parse_mor_tier -> comptage tokens / énoncés"
      pattern: "total_morphemes / total_utterances"
    - from: "SPIKE-REPORT.md"
      to: "LICENSE-VERDICT.md + MOR-CLI-TRIAL.md + résultat MLU"
      via: "synthèse des 3 SPIKE en verdict Go/No-Go"
      pattern: "Go|No-Go"
---

<objective>
Wave 2 du spike : écrire l'amorce de code PROPRE et réutilisable (D-05) — résolution du binaire mor (pattern find_ffmpeg), wrapper subprocess `run_mor`, parseur du tier `%mor`, et calcul de la MLU en morphèmes — avec tests automatisés validant la MLU contre l'oracle MTLN. Puis rédiger le rapport de spike final concluant Go ou No-Go.

Purpose: SPIKE-03 (Success Criterion 3) + le rapport Go/No-Go (Success Criterion 4). Contrairement à l'exploration des Plans 01/02 (jetable, D-06), ce code est l'amorce que la Phase 2 reprend directement (D-05) — il doit être propre, typé, testé. Critère métrique souple (D-08) : "MLU plausible et explicable", pas exacte au centième.
Output: `app/models/clan_wrapper.py` testé + `SPIKE-REPORT.md` (verdict final).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-spike-clan/01-RESEARCH.md
@.planning/phases/01-spike-clan/01-CONTEXT.md
@.planning/phases/01-spike-clan/01-01-SUMMARY.md
@.planning/phases/01-spike-clan/01-02-SUMMARY.md
@app/models/operation_fichier.py

<interfaces>
<!-- Pattern de résolution de binaire à reproduire, depuis app/models/operation_fichier.py:70 -->
```python
def find_ffmpeg():
    if getattr(sys, 'frozen', False):
        base_path = sys._MEIPASS
        return os.path.abspath(os.path.join(base_path, '_internal', 'ffmpeg'))
    vendored = os.path.abspath(os.path.join(os.path.dirname(__file__), '..', 'bin', 'ffmpeg'))
    if os.path.isfile(vendored):
        return vendored
    system = shutil.which('ffmpeg')
    if system:
        return system
    return 'ffmpeg'
```

<!-- Contrats à créer dans app/models/clan_wrapper.py (l'exécuteur implémente CES signatures) -->
```python
def find_clan_mor() -> str: ...
def run_mor(cha_path: str, grammar_dir: str, mor_bin: str | None = None) -> str: ...
def parse_mor_tier(cha_text: str) -> list[list[str]]: ...   # liste d'énoncés, chaque énoncé = liste de tokens POS|lemme
def compute_mlu_morphemes(cha_path: str) -> float: ...
```
</interfaces>

# Plan 01 : verdict licence, fixtures (sample_fra.cha oracle déterministe, mtln_sample.cha + âge/norme).
# Plan 02 : commande mor headless retenue -> arguments subprocess Python (run_mor doit les utiliser).
# Recherche : §Architecture Patterns (find_clan_mor, run_mor, compute_mlu_morphemes), §Format %mor, §Oracle MTLN (normes MLU-m par âge), §Pitfall 2 encodage, §Pitfall 3 overwrite.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parseur %mor + calcul MLU morphèmes (TDD, CI sans CLAN)</name>
  <files>app/models/clan_wrapper.py, tests/test_clan_spike.py</files>
  <read_first>
    - app/models/operation_fichier.py (pattern find_ffmpeg, conventions encodage/docstrings FR)
    - .planning/phases/01-spike-clan/01-RESEARCH.md (§Format du tier %mor ; §Algorithme MLU morphèmes ; règles : tokens `POS|lemme`, exclure xxx/yyy/www, ponctuation non comptée)
    - tests/fixtures/sample_fra.cha (oracle déterministe créé en Plan 01)
  </read_first>
  <behavior>
    RED d'abord — `tests/test_clan_spike.py` (pytest, `from __future__ import annotations`) :
    - `test_parse_mor_tier` : `parse_mor_tier(open('tests/fixtures/sample_fra.cha').read())` retourne 3 énoncés ; le 1er énoncé contient les tokens `det|le`, `n|chat`, `v|manger-PRES&3S` (3 tokens) ; le 3e énoncé = `['n|maman']` (1 token). La ponctuation `.` n'est jamais un token.
    - `test_parse_mor_tier_exclut_xxx` : un énoncé `%mor: xxx` est exclu (0 énoncé valide retourné pour cette ligne).
    - `test_compute_mlu_morphemes_sample` : `compute_mlu_morphemes('tests/fixtures/sample_fra.cha')` retourne une valeur calculable depuis la fixture (3+4+1)/3 ≈ 2.667 (documenter la valeur exacte retenue selon la règle de comptage de `de+det|le`). Tolérance D-08 : assert dans [2.0, 3.5].
    - `test_compute_mlu_morphemes_aucun_enonce` : un .cha sans tier %mor valide lève `ValueError`.
    Ces tests NE nécessitent PAS CLAN -> tournent en CI. Marquer `@pytest.mark.unit`.
  </behavior>
  <action>
    Créer `app/models/clan_wrapper.py` (docstrings FR, type hints sur signatures publiques, PEP 8) :

    `find_clan_mor() -> str` : calque EXACT de `find_ffmpeg` mais pour le binaire mor. Ordre : frozen `_MEIPASS/_internal/clan/mor` -> vendored `app/../bin/clan/mor` -> `shutil.which('mor')` -> littéral `'mor'`. Sur Windows `shutil.which` trouve `mor.exe`.

    `parse_mor_tier(cha_text: str) -> list[list[str]]` : pour chaque ligne commençant par `%mor:`, extraire les tokens via `re.compile(r'\S+\|\S+')` (un token = `POS|lemme` ou `POS|lemme-morph`, inclut les fusions `det:art|de+det|le` comptées comme 1 token car contient `|`). Exclure l'énoncé entier si un token brut ∈ {`xxx`,`yyy`,`www`,`0`}. Retourner la liste des énoncés non vides (chacun = liste de tokens).

    `compute_mlu_morphemes(cha_path: str) -> float` : lit le fichier en `encoding='utf-8'`, appelle `parse_mor_tier`, calcule `round(sum(len(e) for e in enonces) / len(enonces), 3)`. Lève `ValueError("Aucun énoncé valide trouvé dans le tier %mor")` si aucun énoncé.

    Implémenter le minimum pour faire passer les tests RED -> GREEN. Pas de morphème-splitting fin sur `-` (A4 / D-08 : 1 token = 1 unité ; affinable en Phase 2).

    Commits TDD : `test(01-03): failing tests parse_mor_tier + MLU` puis `feat(01-03): clan_wrapper parsing + compute_mlu_morphemes`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_clan_spike.py -m unit -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/models/clan_wrapper.py` contient `def find_clan_mor(`, `def parse_mor_tier(`, `def compute_mlu_morphemes(`
    - `find_clan_mor` contient le test `getattr(sys, 'frozen', False)` (preuve du pattern find_ffmpeg)
    - `python -m pytest tests/test_clan_spike.py -m unit -q` sort 0 (tests parsing + MLU + ValueError verts, SANS CLAN)
    - `test_parse_mor_tier` et `test_compute_mlu_morphemes_sample` présents et verts
  </acceptance_criteria>
  <done>Parseur %mor et calcul MLU morphèmes implémentés proprement, testés en CI sans CLAN, sur la fixture oracle déterministe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wrapper subprocess run_mor + test d'intégration MLU vs oracle MTLN (gated)</name>
  <files>app/models/clan_wrapper.py, tests/test_clan_spike.py</files>
  <read_first>
    - .planning/phases/01-spike-clan/01-02-SUMMARY.md (commande mor retenue + arguments subprocess + encodage)
    - .planning/phases/01-spike-clan/01-01-SUMMARY.md (chemin grammaire fra/, âge enfant MTLN + norme MLU-m cible, présence %mor)
    - .planning/phases/01-spike-clan/01-RESEARCH.md (§Pattern d'appel subprocess `mor` ; §Oracle — table normes MLU-m 24mo~1.86 ... 48mo~4.82 ; §Pitfall 3 overwrite)
  </read_first>
  <behavior>
    - `test_mlu_morphemes_vs_oracle` (`@pytest.mark.integration`, skip si `shutil.which('mor')` est None) :
      copie `tests/fixtures/mtln_sample.cha` vers un `tmp_path` (Pitfall 3 : ne jamais écraser la fixture), exécute `run_mor(copie, grammar_dir)` puis `compute_mlu_morphemes(copie)`, et assert que la valeur est dans la plage normative de l'âge de l'enfant ±0.5 (D-08). Si la fixture MTLN contient DÉJÀ un tier %mor (cf Plan 01), le test peut sauter `run_mor` et parser directement.
      La plage cible (issue de l'âge documenté en Plan 01) est inscrite en constante du test, ex `MLU_M_ATTENDUE = 3.02  # MTLN 30 mois (Parisse & Le Normand 2002)`.
    - `test_run_mor_args` (`@pytest.mark.unit`, mocker subprocess.run via pytest-mock) : vérifie que `run_mor` construit une LISTE d'arguments (jamais `shell=True`) incluant le grammar_dir, force `encoding='utf-8'`, et lève `RuntimeError` si returncode != 0. Ce test tourne en CI sans CLAN.
  </behavior>
  <action>
    Ajouter `run_mor(cha_path: str, grammar_dir: str, mor_bin: str | None = None) -> str` à `clan_wrapper.py` :
    - `mor_bin = mor_bin or find_clan_mor()`.
    - Construire les arguments EXACTEMENT selon la commande retenue au Plan 02 (liste d'args, p.ex. `[mor_bin, '+l', grammar_dir, '+1', cha_path]` ; régler `env['MORLIB']` si le Plan 02 a retenu l'approche env var).
    - `subprocess.run(args, capture_output=True, text=True, encoding='utf-8', env=env)` — JAMAIS `shell=True` (T-01-05).
    - `if result.returncode != 0: raise RuntimeError(f"mor a échoué: {result.stderr}")`.
    - Retourner `cha_path` (mor `+1` écrit dans le fichier d'entrée — appeler sur une COPIE).
    - Docstring FR documentant l'overwrite `+1` (avertir l'appelant de passer une copie).

    Implémenter le test mock (unit) + le test intégration (gated). RED -> GREEN.
    Commits : `test(01-03): failing run_mor + oracle integration` puis `feat(01-03): run_mor subprocess wrapper`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_clan_spike.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/models/clan_wrapper.py` contient `def run_mor(` utilisant une liste d'arguments et `encoding='utf-8'`, sans `shell=True` (vérifiable : `Select-String "shell=True" app/models/clan_wrapper.py` ne retourne rien)
    - `test_run_mor_args` vert en CI (mock, sans CLAN)
    - `test_mlu_morphemes_vs_oracle` présent ; SKIPPED si mor absent, PASSED si mor présent avec MLU dans la plage normative ±0.5 (D-08)
    - `python -m pytest tests/test_clan_spike.py -q` sort 0 (verts + skips, aucun échec)
  </acceptance_criteria>
  <done>Wrapper subprocess propre et sûr (liste d'args, UTF-8, pas de shell), MLU validée vs oracle MTLN quand CLAN présent.</done>
</task>

<task type="auto">
  <name>Task 3: Rédiger le rapport de spike Go/No-Go (SPIKE-REPORT.md)</name>
  <files>.planning/phases/01-spike-clan/SPIKE-REPORT.md, .planning/phases/01-spike-clan/01-VALIDATION.md</files>
  <read_first>
    - .planning/phases/01-spike-clan/artifacts/LICENSE-VERDICT.md (SPIKE-01)
    - .planning/phases/01-spike-clan/artifacts/MOR-CLI-TRIAL.md (SPIKE-02)
    - .planning/phases/01-spike-clan/01-01-SUMMARY.md et 01-02-SUMMARY.md
    - .planning/phases/01-spike-clan/01-CONTEXT.md (D-07 licence stricte, D-08 métrique souple ; §Deferred : stratégies de repli à documenter ICI)
  </read_first>
  <action>
    Créer `.planning/phases/01-spike-clan/SPIKE-REPORT.md` (en français) — le livrable final (Success Criterion 4). Sections EXACTES :
    - `# Rapport de Spike — Phase 1 (CLAN)`
    - `## SPIKE-01 — Licence` : reprendre le verdict de LICENSE-VERDICT.md (binaire CLAN BSD-3 ; statut grammaire MOR FR). Critère D-07 strict.
    - `## SPIKE-02 — Exécution mor headless` : reprendre MOR-CLI-TRIAL.md (commande retenue OU échec).
    - `## SPIKE-03 — Parsing + MLU morphèmes` : valeur MLU calculée sur l'échantillon MTLN, plage normative d'âge comparée, écart, jugement "plausible et explicable" (D-08). Citer la sortie de `python -m pytest tests/test_clan_spike.py -q`.
    - `## Verdict` : conclusion contenant le mot `Go` ou `No-Go`. Règle : `Go` si SPIKE-01 (licence libre, D-07) ET SPIKE-02 (mor headless OK) ET SPIKE-03 (MLU plausible) ; sinon `No-Go` (préciser le critère bloquant). Si licence grammaire en attente -> `Go conditionnel` avec condition explicite.
    - `## Stratégie de repli (si No-Go ou conditionnel)` : documenter les pistes (demande de permission écrite à TalkBank ; ré-implémentation MOR-lite via Démonette/UD ; Batchalign2/Stanza pour UD tagging ; amélioration ciblée de l'heuristique existante). C'est ICI que les stratégies de repli sont documentées (Deferred dans CONTEXT.md).

    Mettre à jour le frontmatter de `.planning/phases/01-spike-clan/01-VALIDATION.md` : `wave_0_complete: true` et `nyquist_compliant: true`.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if (Select-String -Path '.planning/phases/01-spike-clan/SPIKE-REPORT.md' -Pattern '## Verdict','Go|No-Go','## Stratégie de repli' -Quiet) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - `SPIKE-REPORT.md` contient les 5 sections (SPIKE-01, SPIKE-02, SPIKE-03, Verdict, Stratégie de repli)
    - `## Verdict` contient `Go` ou `No-Go`
    - Le rapport cite la valeur MLU calculée et la plage normative MTLN (preuve SPIKE-03)
    - `01-VALIDATION.md` a `wave_0_complete: true` et `nyquist_compliant: true`
  </acceptance_criteria>
  <done>Rapport Go/No-Go écrit synthétisant les 3 SPIKE avec stratégie de repli documentée — la Phase 2 a sa décision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chemins fichier/grammaire -> args subprocess mor | chemins passés au binaire tiers |
| fichier .cha (sortie mor) -> parseur Python | texte annoté lu et décodé |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering | injection d'argument / commande via chemin | mitigate | `run_mor` utilise une LISTE d'arguments subprocess, jamais `shell=True` ni concaténation de chaîne (acceptance criteria vérifie l'absence de `shell=True`). |
| T-01-08 | Denial of service | sortie mor volumineuse / blocage | accept | Spike local sur fichier .cha de test borné ; pas d'entrée distante. Timeout subprocess affinable en Phase 2. |
| T-01-09 | Information disclosure | décodage sortie / logs | mitigate | `encoding='utf-8'` forcé ; échantillon CHILDES public (pas de patient) donc loggable. Aucune donnée patient dans ce spike. |
| T-01-10 | Tampering | résolution binaire mor (find_clan_mor) | mitigate | Ordre de résolution contrôlé (frozen/vendored avant PATH) calqué sur find_ffmpeg ; en Phase 6 le binaire sera embarqué et figé dans le bundle. |
</threat_model>

<verification>
- `python -m pytest tests/test_clan_spike.py -q` : tous verts ou skipped (aucun échec).
- `clan_wrapper.py` expose find_clan_mor / run_mor / parse_mor_tier / compute_mlu_morphemes, sans `shell=True`.
- SPIKE-REPORT.md conclut Go/No-Go avec stratégie de repli.
</verification>

<success_criteria>
- SPIKE-03 satisfait : MLU morphèmes calculée et plausible vs oracle (Success Criterion 3, D-08).
- Rapport Go/No-Go écrit (Success Criterion 4).
- Amorce de code propre et testée prête pour la Phase 2 (D-05).
</success_criteria>

<output>
After completion, create `.planning/phases/01-spike-clan/01-03-SUMMARY.md` incluant : verdict final Go/No-Go, MLU calculée vs norme, état des tests (verts/skips), et la liste des fonctions réutilisables livrées dans clan_wrapper.py pour la Phase 2.
</output>
